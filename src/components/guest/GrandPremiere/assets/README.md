# Grand Premiere — `assets/`

Phase 1 (architecture only) — intentionally empty.

Reserved for this template's own static assets (icons, decorative SVGs,
a template-default cover image if one is added later via
`lib/coverDefaults.js`). No couple/invitation data ever belongs here —
that always comes from the Builder (`data`), never hardcoded.
